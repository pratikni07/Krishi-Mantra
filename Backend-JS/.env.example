# Copy to .env alongside docker-compose.yml and fill with real values.
# .env is gitignored; .env.example is safe to commit.

# MongoDB root credentials (used by the mongodb container and every service)
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=change-me-before-deploy
